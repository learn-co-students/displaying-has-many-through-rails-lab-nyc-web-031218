<h1>Name: <%= @doctor.name %></h1>
<h3>Department: <%= @doctor.department %></h3>
<ul>
  <% @doctor.appointments.each do |a| %>
    <li><%= a.readable_date %> with <%= link_to a.patient.name, a.patient %></li>
  <% end %>
</ul>
