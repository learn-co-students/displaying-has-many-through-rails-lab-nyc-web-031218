<h1><%= "#{@doctor.name} -- #{@doctor.department}"  %></h1>

<% @doctor.appointments.each do |appt| %>
  <%= appt.time_string %>
  <%= "--" %>
  <%= link_to appt.patient_name, patient_path(appt.patient) %><br>
<% end %>
